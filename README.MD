## Запуск проекта

1. Клонируем репозиторий `git clone link-to-repo`
2. Устанавливаем зависимости: `npm install`
3. Настроить переменные окружения в .env файле: пример в .env-example
4. Запуск проекта `npm run dev` - запускает backend
